<form method="post" action="{{ path('pharmacy_availabe_drugs_delete', {'id': pharmacy_availabe_drug.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pharmacy_availabe_drug.id) }}">
    <button class="btn">Delete</button>
</form>
