
            <div class="row">

                <div class="form-header mb-15">
                    <h1>Modifier ce rendez-vous</h1>
                </div>
                {{form_start(form_edit, { 'attr': {'class': 'col-md-12'}})}}
                <h3 class="mt-15">Date du début</h3>
                         <div class="row">
                            <div class="col-md-12 m-auto">
                                <div class="form-group">
                              {{form_widget(form_edit.beginAt)}}
                                </div>
                            </div>
                        </div>
                            <div class="row">
                                <div class="col-md-6 col-lg-12">
                                    <div class="form-group">
                                  {{form_widget(form_edit.daysOfWeek)}}
                                    </div>
                                </div>
                            <div class="col-md-6 col-lg-12">
                                <div class="form-group">
                              {{form_widget(form_edit.isConfirmed)}}
                                </div>
                            </div>
                         </div> 
                        <div class="form-btn">
                            <button class="submit-btn">Modifier</button>
                        </div>
                {{form_end(form_edit)}}
        
        </div>
