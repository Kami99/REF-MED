{{ form_start(form) }}
            <div class="row">
                <div class="col-md-12 col-lg-12  m-auto">
                    <div class="form-group">
                {{form_row(form.isAvailable, { 'attr': {'class': 'form'}})}}
                    </div>
                </div>
        </div> 
        <div class="row">
            <div class="col-md-6 col-lg-12 m-auto">
                <div class="form-group">
            {{form_row(form.price, { 'attr': {'class': 'form'}})}}
                </div>
            </div>
        </div> 
        <div class="row">
            <div class="col-md-6 col-lg-12 m-auto">
                <div class="form-group">
            {{form_row(form.drugs)}}
                </div>
            </div>
        </div> 
        <div class="form-btn">
            <button class="submit-btn">Valider</button>
        </div>
{{ form_end(form) }}
